streamlit>=1.38
pandas>=2.0
numpy>=1.26
plotly>=5.20
scikit-learn>=1.4
joblib>=1.3

# Only needed by train_severity_model.py, and only if your CSV doesn't already
# have a desc_lemma column (the notebook's saved CSVs do, so this is rarely hit).
nltk>=3.8
